'use strict';

const Application = require('./app');
const KoaApplication = require('./koa');
const SocketApplication = require('./socket');

module.exports = {
  Application,
  KoaApplication,
  SocketApplication
};
