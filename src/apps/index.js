'use strict';

const Application = require('./app');
const KoaApplication = require('./koa');
const SocketApplication = require('./koa');

module.exports = {
  Application,
  KoaApplication,
  SocketApplication
};
